# 계획 — "Canton DvP" 단계별 학습 코스 (백엔드 개발자 대상)

> 다음 세션에서 이 계획대로 작성한다. (이 파일 = 세션 리셋 후 핸드오프 문서)
> v3 — bitcoin-study(S0~S13) 목차 확인 후 교육 설계 반영.

## 확정된 방향 (사용자 결정)
1. **청중**: Canton을 모르는 **백엔드 개발자**. 블록체인 기본은 앎(tx·블록·스마트컨트랙트·지갑·가스). → **이더리움 비교**를 매 챕터 사이드박스로.
2. **위치/구성**: `wiki/` 안 **신규 폴더** + **스테이지별 파일**(단일 페이지 아님).
3. **형식**: 개념 설명서 + **교육용**(각 챕터에 강의 노트). 학습 경험은 **bitcoin-study.vercel.app/learn** 스타일(스테이지 클릭·점진·간단 실습/애니).
4. **기존 자료 재사용 금지**: `wiki/notes/*`를 링크로 엮지 않음. 내용은 **새 파일로 옮겨 코스 흐름에 맞게 재작성**(소스 참고만).
5. **대외비**: 외부 공유용 일반화 — 회사·Musubi·POC 실명 금지, 역할 기반.

## 레퍼런스 목차 (bitcoin-study, 캡처 확인)
S0 Alice→Bob · S1 Keys & Address · S2 Seed & Wallet Types · S3 Transaction · S4 UTXO · S5 Block · S6 Network · S7 Blockchain · S8 PoW Mining · S9 Mining Strategy · S10 Node Structure · S11 Node Operation · S12 Scalability · S13 Privacy

### 차용할 교육 패턴
- **하나의 시나리오를 끝까지**: "Alice→Bob 1 BTC"가 전 챕터를 관통 → 우리는 **"기관 A⇄B 통화 맞교환(FX DvP)" 한 건**을 관통시킨다.
- **질문으로 연다**: 각 스테이지를 "~는 어떻게 ~될까?" 질문으로 시작.
- **점진 빌드업**: 행동 → 소유권 → 규칙 → 저장 → 특성 → 운영.
- **프라이버시 위치 역전**: 비트코인은 S13(끝·각주). **우리는 프라이버시·원자성을 중앙(핵심 차별)으로.** 내러티브 훅으로 활용.

## 폴더·파일 구조
```
wiki/dvp-course/
├─ s00-opening.md        # A⇄B 한 건의 정산 (시나리오 던지기 + 코스 맵 + ETH 멘탈모델 전환)
├─ s01-counterparty-risk.md  # 왜 어려운가: "누가 먼저 보내?"
├─ s02-party-ownership.md     # 누가 이 자산을 움직일 수 있나 (파티·키·권한)
├─ s03-daml-contract.md       # 정산 규칙을 코드로 (템플릿·초이스·권한)
├─ s04-nodes-ledger.md        # 이 기록은 어디에 저장되나 (참여자 노드·원장)
├─ s05-privacy.md             # 제3자는 못 본다 (부분 트랜잭션 프라이버시) ★차별①
├─ s06-atomicity-dvp.md       # 전부 아니면 전무 (원자성·DvP) ★차별②
├─ s07-settlement-flow.md     # 한 건이 실제로 도는 길 (제안→수락→잠금→실행)
├─ s08-tokens-registry.md     # 교환하는 통화는 어디서 (발행자=레지스트리·브릿지 vs 직접발행)
├─ s09-operations.md          # 누가 무엇을 운영·설치하나
├─ s10-finality-consensus.md  # 되돌릴 수 없나·누가 순서를 (Synchronizer·BFT·확정성)
└─ s11-recap.md               # 정리·직접 해보기·더 배우기
```
- `sNN-` 접두사로 사이드바 순서 보장. 폴더는 `gen_sidebar.py` 자동 인식. `wiki/overview/learn/`(공식 번역)와 구분.

## 관통 시나리오 (모든 챕터 공통)
> **기관 A가 통화 A를, 기관 B가 통화 B를 들고 있고, 둘을 맞바꾼다(FX DvP).** 운영사(venue)가 정산을 조율한다.
이 한 건의 정산이 소유권→규칙→저장→프라이버시→원자성→흐름→토큰→운영→확정성을 차례로 불러낸다.

## 각 챕터 공통 틀 (스테이지 1장)
1. **❓ 이 단계의 질문** (한 줄, 호기심 유발)
2. **개념** (백엔드 개발자 눈높이, 짧게 — 관통 시나리오에 붙여서)
3. **🔷 이더리움이라면** (대비 박스 — 거의 매 챕터)
4. **다이어그램** (mermaid)
5. **🎤 강의 노트** (어떻게 설명할지: 핵심 한 문장·비유·무엇을 보여주며 짚을지·예상 질문&답)
6. **→ 다음 단계** (브리지 한 줄)

## 챕터별 핵심 (outline — 새 세션에서 확정)
- **S0 오프닝**: "A가 B와 통화를 맞바꾸려 한다. 이 한 건이 무엇을 불러낼까?" 코스 맵 + "이더리움 개발자라면 멘탈모델을 이렇게 바꾸세요"(account/EVM/공개 → 파티/Daml/프라이빗) + "왜 이더리움이 아니라 Canton?" 떡밥.
- **S1 카운터파티 리스크**: "누가 먼저 보내?" 떼임 위험(Herstatt). 🔷 이더리움 atomic swap/escrow의 한계(공개·기관 부적합).
- **S2 파티 & 소유권**: "A가 이 자산을 쓸 수 있다는 건 어떻게 증명되나?" 파티·키·signatory/observer. 🔷 EOA/주소·서명.
- **S3 Daml 컨트랙트**: "정산 규칙은 어떻게 코드가 되나?" 템플릿·초이스·권한 모델. 🔷 솔리디티(누구나 호출) vs Daml(권한·이해관계자 명시).
- **S4 참여자 노드 & 원장**: "이 정산 기록은 어디에 저장되나?" 노드는 자기 파티 컨트랙트만. 🔷 이더리움 글로벌 복제 상태.
- **S5 프라이버시 ★차별①**: "A·B의 거래를 제3자가 볼까?" 부분 트랜잭션 프라이버시(뷰 분해). 🔷 이더리움 투명성·mempool. (훅: "비트코인은 이걸 맨 끝 각주로 뒀지만, 기관 정산에선 출발점")
- **S6 원자성 & DvP ★차별②**: "양쪽이 동시에, 아니면 둘 다 안 되게?" 한 트랜잭션 다리 묶기. 🔷 EVM atomic composability(되지만 공개) → Canton은 *프라이버시와 결합된* 원자성.
- **S7 정산 흐름**: "한 건이 실제로 어떻게 도나?" 제안→수락→개시→양측 잠금→원자적 실행 (시퀀스 다이어그램, 관통 시나리오로).
- **S8 토큰 & 레지스트리**: "맞바꾸는 그 통화는 어디서 왔나?" 발행자=레지스트리(발행/소각·증빙). 외부발행+브릿지(래핑) vs Canton 직접발행 — flow·신뢰지점.
- **S9 운영 & 배포**: "이걸 누가 운영하나?" 참여기관/운영사(venue)/발행자. "정산 코드(DAR)는 모든 노드, 운영사 백엔드·venue 파티는 운영사만". 자체호스팅 vs NaaS.
- **S10 확정성 & 합의**: "되돌릴 수 없나? 누가 순서를 정하나?" Synchronizer(시퀀서+미디에이터)·BFT·즉시 결정적 확정. 🔷 비트코인 확률적·이더리움 PoS finality.
- **S11 정리·실습·심화**: 차별점 3(프라이버시·원자성·신원/확정성) 요약 + 직접 해보기(인터랙티브 데모) + 더 배울 거리.

## 참고 소스 → 새 파일로 재작성 (링크 재사용 X)
| 스테이지 | 내용 소스(참고만) |
|---|---|
| S1·S6 | `wiki/notes/atomic-dvp-real-differentiator.md` |
| S0·S4·S10 | `wiki/notes/blockchain-layers-l0-l1-l2.md`, `btc-ethereum-canton-compare.md` |
| S5 | (위 + 프라이버시 모델) |
| S7 | `dev/docs/architecture-b2b-b2c-multichain.md`(시퀀스) |
| S8 | `wiki/notes/token-issuance-bridge-patterns.md` |
| S9 | `wiki/notes/dvp-settlement-app-architecture.md` |
| 용어 | `wiki/glossary.md`, `notes/term-cheatsheet.md` |
> 소스로만 본다. 코스 챕터는 백엔드 개발자·이더리움 비교·관통 시나리오·스테이지 흐름에 맞게 **새로 쓴다**.

## 인터랙티브(bitcoin-study식) — 2단계
- **Phase 1 (먼저)**: 스테이지형 마크다운 코스(개념+ETH 비교+mermaid+강의 노트). 사이드바로 한 단계씩.
- **Phase 2 (선택, Phase 1 확정 후)**: 핵심 스테이지(S5 프라이버시·S7 정산)에 **자기완결 인터랙티브 HTML**(클릭·애니, 무백엔드 단일 파일, 일반화) 추가. `dev/demo/`는 로컬·프로젝트색이라 그대로 공유 불가 → 일반화 버전 별도 제작.

## 작성 규칙 (CLAUDE.md 위키 규칙)
- 각 챕터 frontmatter(title, type: note, translated, status, tags) + 상단 ⚠️ 배너(학습 코스, 번역본 아님).
- 회사·Musubi·POC 실명 금지 → 역할 기반(기관 A/B·운영사·발행자), 통화 일반화.
- mermaid 표준 펜스 ```mermaid.
- 새 폴더/파일 → `wiki/index.md`에 코스 섹션 등록(+추천 학습순서) → `gen_sidebar.py`·`gen_nav.py`·`gen_tooltips.py` 재생성.
- 새 용어는 `glossary.md` 툴팁 표에 먼저 추가.
- `wiki/log.md` 이력.

## 작업 순서 (새 세션)
1. 이 계획 + 참고 소스 훑기.
2. **S0~S11 outline 확정**(사용자 확인) → 챕터 작성(관통 시나리오 + ETH 비교 박스 + 강의 노트 + 다이어그램).
3. index 등록 + 스크립트 재생성 + 툴팁 + log.
4. 백엔드 개발자 관점 검토 → 커밋·푸시.
5. (Phase 2 논의) 인터랙티브 HTML 스테이지.

## 새 세션 킥오프 프롬프트 (그대로 붙여넣기)
> `dev/docs/explainer-doc-plan.md`(v3)를 읽고, 그 계획대로 **Canton을 모르는 백엔드 개발자용 "Canton DvP" 단계별 학습 코스**를 `wiki/dvp-course/`에 **스테이지별 파일**(s00~s11)로 만들어줘. bitcoin-study처럼 **하나의 시나리오(기관 A⇄B 통화 맞교환)를 끝까지 끌고가고**, 각 챕터는 질문으로 열고 **🔷 이더리움 비교 박스**와 **🎤 강의 노트**를 넣어줘. **기존 wiki/notes는 링크 재사용하지 말고** 새 파일로 옮겨 **재작성**해. 회사·고유명 빼고 역할 기반. 위키 규칙(인덱스·스크립트·툴팁) 지키고, **먼저 S0~S11 outline을 보여주고 확정받은 뒤** 작성해줘.
